import React from "react";

function App() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>🚀 React + Docker + Jenkins + AWS ECR</h1>
      <p>This app is successfully running from a Docker container!</p>
    </div>
  );
}

export default App;
